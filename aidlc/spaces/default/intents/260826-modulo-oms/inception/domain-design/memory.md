<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-09-16T00:00:00Z — Domain Design corre tras el skip de refined-mockups. Consume requirements.md v2 (14 FR), stories.md (34 historias) y el codekb brownfield. Referencia de UI = prototipo React `src/pages/oms/` (refined-mockups superada). Las ~20 decisiones firmes de project.md ## Decided ya acotan casi todas las fronteras: OMS termina en "alistado"; núcleo compartido (pedido/ruta/chofer/camión) es del TMS, el OMS lo consume; tablas propias del OMS = `route_dispatch_schedule`, `order_priority_*`, motor de prioridad; una Lambda por compañía; motor de reglas = catálogo; Simulador = entidad persistida; IA/Bedrock para observaciones; columnas compañía/país; BD `logistica_olo` esquemas OMS/TMS. Las preguntas se limitan a decisiones reales de descomposición con defaults.
- 2026-09-16T00:00:00Z — Observación: el codekb `architecture.md` (§6 posicionamiento objetivo) muestra el modelo VIEJO ("lago de datos", OMS→lago); es codekb brownfield stale (drift ya reportado). Superado por project.md v2 (no lago; escritura a nivel WMS/EFLOW). Se trata como lectura del código actual, NO como target de diseño.
- 2026-09-16T00:00:00Z — Interpretación (Q1=C): CalculadorScore se separa como componente puro por dos motivos firmes combinados — regla Testing Posture (project.md 2026-08-28, cálculo de prioridad en módulos puros testeables) + score desde 1ª entrega (DECIDED 2026-09-14). Resultado: 10 componentes de dominio + 5 de UI.
- 2026-09-16T00:00:00Z — Interpretación (Q4=A + matiz): el lado LECTURA de EFLOW se modela DENTRO de ColaCandidatos (adaptador de lectura: expedición_cabecera fecha_de_cierre IS NULL + DISP sin NUMEROVIAJEWMH, contra réplica) y el lado ESCRITURA dentro de EscritorEflow (DISP→GENERADA + prioridad, atómica, solo WMS/EFLOW). EFLOW/Maestro/TMS/Bedrock quedan como external_dependencies, sin componentes-adaptador extra.
- 2026-09-16T00:00:00Z — Tradeoff: el motor se descompuso en 6 bloques (cola, orquestador, score, 2 reglas, escritor) en vez de un monolito, aceptando más coordinación a cambio de testabilidad por regla y encapsular los invariantes de lectura/escritura en un punto cada uno. El Simulador reusa el motor (no duplica el cálculo) para evitar divergencia.
- 2026-09-16T00:00:00Z — Sensores: required-sections PASS (components.md 4 H2, decisions.md 9 H2); upstream-coverage PASS (requirements/stories/architecture/component-inventory todos referenciados). traceability = FALSO POSITIVO advisory conocido: el sensor busca IDs `USx.y` (con punto) pero stories.md usa `US1..US33` sin sub-nivel; reporta "stories.md contains no traceable IDs". Es advisory (no bloquea), mismo patrón que el learned 2026-09-01 de units-generation. traceability.json queda poblado y correcto a nivel US→componente.
- 2026-09-16T00:00:00Z — Review advisory (architecture-reviewer, iteración 1): READY, 0 bloqueantes, 0 mayores, 3 menores (insumo para Functional Design, no bloquean): (1) FR14.3/US33 "toda escritura registra usuario" — solo MotorPriorizacion→Auditoria está modelado; falta decidir en Functional Design si las escrituras de CatalogoReglas (US17/US18 cambio de regla) y del Simulador (aplicar) pasan por Auditoria (añadiría aristas) o son transversales. (2) CalendarioRutas/RutaDespacho: aclarar si RutaDespacho es entidad propia o read-model/caché del TMS (verdad en TMS + CRUD gated diferido). (3) PedidoCandidato→RegistroPrioridad: confirmar que es lectura por la Cola, no acoplamiento inverso (Cola es dependent de Motor). DAG limpio, simetría OK, dueños únicos OK, invariantes firmes sin violación, cobertura FR/US completa.
